{
  "name": "market-world",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client-next",
    "build": "npm run build --prefix client-next",
    "start": "npm start --prefix client-next"
  }
}
